/* Supabase の接続情報。
   Supabase ダッシュボード → Project Settings → API から:
     - Project URL       → SUPABASE_URL
     - Project API keys の "anon public" → SUPABASE_ANON_KEY
   をコピーして差し替えてください。両方空のあいだは「ログイン準備中」表示になります（サイトは普通に使えます）。 */
window.AUTH_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
